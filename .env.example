# Backend URL is in vite.config.js proxy only - no VITE_API_BASE_URL needed for dev
# Optional: For Add User to work when backend has no login, add a valid JWT:
# VITE_DEV_TOKEN=your-jwt-from-backend
