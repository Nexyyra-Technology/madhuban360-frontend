# Backend is https://madhuban360-backend.onrender.com (default in code).
# - Dev: Leave VITE_API_BASE_URL unset → Vite proxy forwards /api to the live backend.
# - Production build: Uses live backend by default; set below only to override.
# VITE_API_BASE_URL=https://madhuban360-backend.onrender.com
# For local backend instead: VITE_PROXY_TARGET=http://localhost:3000 (vite.config.js)
# Optional: VITE_DEV_TOKEN=your-jwt (used only when backend returns 404 on login)
