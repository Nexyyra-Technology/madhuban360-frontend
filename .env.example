# Backend URL (required to hit your deployed API)
# Set this so all API calls go to your backend instead of through Vite proxy.
VITE_API_BASE_URL=https://madhuban360-backend.onrender.com

# Optional: fallback token when backend returns 404 on login (dev only)
# VITE_DEV_TOKEN=your-jwt-token