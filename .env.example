# Option A (proxy): Leave VITE_API_BASE_URL unset; Vite proxies /api to backend.
#   For local backend: VITE_PROXY_TARGET=http://localhost:3000 (then restart dev server)
# Option B (direct): Set so fetch goes to backend (fixes PUT /api/... hitting frontend):
#   VITE_API_BASE_URL=http://localhost:3000
# Production (Vercel): MUST set:
#   VITE_API_BASE_URL=https://madhuban360-backend.onrender.com
# Optional: For Add User when backend has no login:
#   VITE_DEV_TOKEN=your-jwt-from-backend
